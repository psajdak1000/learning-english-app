server:
  port: 8080
spring:
  application.name: englishapp
  h2.console.enabled: true
  datasource:
    url: jdbc:h2:mem:englishdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate.ddl-auto: update
    open-in-view: false
